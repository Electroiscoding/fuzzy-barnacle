# Todo App

This is a simple todo application built with React Native and Expo. It allows users to add, complete, and delete todos. The app also features local notifications, which are scheduled to appear a few seconds after a new todo is created.

## Get started

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npx expo start
   ```
